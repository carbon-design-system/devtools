import { components } from './components';
import { selectors } from './selectors';
import { tokens } from './tokens';

window.bxdev = {
  components,
  selectors,
  tokens,
};

console.log(window.bxdev);
