import { colors } from '@carbon/colors';
import { fontWeights, styles } from '@carbon/type';
import { breakpoints, spacing, layout } from '@carbon/layout';
import { themes } from '@carbon/themes';

const aspectRatios = ['16:9', '9:16', '2:1', '1:2', '4:3', '3:4', '1:1']; // TODO: waiting on exports to be available from carbon

export default {
  colors,
  fontWeights,
  styles,
  aspectRatios,
  breakpoints,
  spacing,
  layout,
  themes,
};
