name: PR Check

on:
  push:
    branches: [ base ]
  pull_request:
    branches: [ base ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
  
    - name: Install dependencies
      run: yarn
    - name: Build project
      run: yarn build
    - name: carbon-devtools
      run: yarn test --scope=carbon-devtools --if-present
    - name: devtools-utilities
      run: yarn test --scope=@carbon/devtools-utilities --if-present
    - name: devtools-token-server
      run: yarn test --scope=@carbon/devtools-token-server --if-present
    - name: devtools-demo-site
      run: yarn test --scope=@carbon/devtools-demo-site --if-present
